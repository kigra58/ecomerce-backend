class CartService {
  /**
   *  PRODUCT LIST
   */
  async getCartItems({}) {
    try {
    } catch (error) {
      console.error(error);
    }
  }

  /**
   *  UPDATE CART ITEMS
   */
  async updateCartItems({}) {
    try {
    } catch (error) {
      console.error(error);
    }
  }

  /**
   *   DELETE ITEM
   */
  async deleteCartItem({}) {
    try {
    } catch (error) {
      console.error(error);
    }
  }
}
export default new CartService();
