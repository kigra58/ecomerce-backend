

class ProductService {
   
    /**
     *  PRODUCT LIST
     */
   async productList ({})  {
     try {
        
     } catch (error) {
        console.error(error)
     }
   } 

   
    /**
     *  PRODUCT DETAILS
     */
   async productDetails ({})  {
     try {
        
     } catch (error) {
        console.error(error)
     }
   } 

    /**
     *  ADD NEW PRODUCT 
     */
    async addProduct ({})  {
     try {
        
     } catch (error) {
        console.error(error)
     }
   } 

};

export default new ProductService()

