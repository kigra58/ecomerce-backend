class AuthService {
  /**
   * SIGNUP
   */
  async signup({}) {
    try {
    } catch (error) {
      console.error(error);
    };
  };

  /**
   * LOGIN
   */
  async login({}) {
    try {
    } catch (error) {
      console.error(error);
    };
  };

  /**
   * FORGOT PASSWORD
   */
  async forgotPassword({}) {
    try {
    } catch (error) {
      console.error(error);
    };
  };

  /**
   * CHANGE PASSWORD
   */
  async changePassword({}) {
    try {
    } catch (error) {
      console.error(error);
    };
  };
}

export default new AuthService();
